{% extends 'base.html.twig' %}

{% block title %} - Usuń książkę "{{ book.title }}"{% endblock %}

{% block aside %}
	{% include('includes/aside.html.twig') %}
{% endblock %}

{% block main %}
	<section class="section">
		<h2 class="section__header">{{ book.name }}</h2>

        <p class="mb-3">Czy na pewno chcesz usunąć tą książkę? Ta akcja jest nieodwracalna.</p>

        <form action="{{ path('book_delete', {'id': book.id}) }}" method="post" class="d-inline-block mr-2">
			<input type="hidden" name="_method" value="DELETE">
			<input type="hidden" name="_token" value="{{ csrf_token('book_delete' ~ book.id) }}">
			<button type="submit" class="btn btn-danger d-inline-block">Usuń</button>
		</form>

		<a href="{{ path('book_show', {'id': book.id}) }}" class="mt-3">
			Pokaż dane książki
		</a>
	</section>
{% endblock %}