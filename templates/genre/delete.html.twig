{% extends 'base.html.twig' %}

{% block title %} - Usuń gatunek "{{ genre.name }}"{% endblock %}

{% block aside %}
	{% include('includes/aside.html.twig') %}
{% endblock %}

{% block main %}
	<section class="section">
		<h2 class="section__header">{{ genre.name }}</h2>

        <p class="mb-3">Czy na pewno chcesz usunąć ten gatunek? Ta akcja jest nieodwracalna.</p>

        <form action="{{ path('genre_delete', {'id': genre.id}) }}" method="post" class="d-inline-block mr-2">
			<input type="hidden" name="_method" value="DELETE">
			<input type="hidden" name="_token" value="{{ csrf_token('genre_delete' ~ genre.id) }}">
			<button type="submit" class="btn btn-danger d-inline-block">Usuń</button>
		</form>

		<a href="{{ path('genre_show', {'id': genre.id}) }}" class="mt-3">
			Pokaż gatunek
		</a>
	</section>
{% endblock %}
