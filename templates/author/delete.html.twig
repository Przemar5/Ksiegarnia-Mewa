{% extends 'base.html.twig' %}

{% block title %} - Usuń autora "{{ author }}"{% endblock %}

{% block aside %}
	{% include('includes/aside.html.twig') %}
{% endblock %}

{% block main %}
	<section class="section">
		<h2 class="section__header">{{ author }}</h2>

        <p class="mb-3">Czy na pewno chcesz usunąć tego autora? Ta akcja jest nieodwracalna.</p>

        <form action="{{ path('author_delete', {'id': author.id}) }}" method="post" class="d-inline-block mr-2">
			<input type="hidden" name="_method" value="DELETE">
			<input type="hidden" name="_token" value="{{ csrf_token('author_delete' ~ author.id) }}">
			<button type="submit" class="btn btn-danger d-inline-block">Usuń</button>
		</form>

		<a href="{{ path('author_show', {'id': author.id}) }}" class="mt-3">
			Pokaż dane autora
		</a>
	</section>
{% endblock %}
