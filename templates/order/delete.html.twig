{% extends 'base.html.twig' %}

{% block title %} - Usuń zamówienie{% endblock %}

{% block main %}
	<section class="section">
		<h2 class="section__header mb-3">Czy na pewno chcesz usunąć to zamówienie?</h2>

		<form method="post" class="mt-2 mb-3">
			<input type="hidden" name="_method" value="delete">
			<input type="hidden" name="_token" value="{{ csrf_token('order_delete' ~ order.id) }}">
			<button type="submit" class="btn btn-danger">Usuń</button>
		</form>
	</section>
{% endblock %}